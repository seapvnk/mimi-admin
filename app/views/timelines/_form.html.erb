<%= form_with(model: timeline) do |form| %>
  <% if timeline.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(timeline.errors.count, "error") %> prohibited this timeline from being saved:</h2>

      <ul>
        <% timeline.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :parent_id, style: "display: block" %>
    <%= form.collection_select :parent_id, @timelines, :id, :id, prompt: "Select a parent timeline" %>
  </div>
  <br>

  <div>
    <%= form.submit %>
  </div>
<% end %>
